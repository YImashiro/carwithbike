from Module import recog
import threading

left_coord,center_coord,right_coord = recog.setting()
th1 = threading.Thread(target=recog.capturevideo)
th1.start()
while True:
    angle = recog.coordtoangle(left_coord[0],center_coord[0],right_coord[0],recog.grav[0])
    print(angle)

        
